import PropTypes from 'prop-types';
const ReadBooks = () => {
    // const booksRead = useActionData()
    // const { id } = useParams();
    return (
        <div>
            jdfh
        </div>
    );
};

ReadBooks.propTypes = {
    data: PropTypes.object
}
export default ReadBooks;